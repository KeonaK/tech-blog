<!-- Loop over each object in the creates array -->
{{#each creates as |create| }}
<div class="row mb-4 create">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/create/{{create.id}}">{{create.name}}</a>
    </h2>
    {{!-- <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount create.needed_funding}}</span>
      needed!
    </p> --}}
    <p>Created by {{create.user.name}} on {{format_date create.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{create.description}}
    </p>
  </div>
</div>
{{/each}}